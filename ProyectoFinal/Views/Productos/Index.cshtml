@model IEnumerable<Proyecto.Models.Productos>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create" class="btn btn-primary">Agregar Producto</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Existencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descontinuado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categorias)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <img src="data:image/jpeg;base64,@(Convert.ToBase64String(item.Image))">
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Existencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descontinuado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categorias.NombreCategoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductoID" class="btn btn-outline-primary btn-sm">Editar</a> 
                <a asp-action="Details" asp-route-id="@item.ProductoID" class="btn btn-outline-success btn-sm">Detalles</a> 
                <a asp-action="Delete" asp-route-id="@item.ProductoID" class="btn btn-outline-danger btn-sm">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
